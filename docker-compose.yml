version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432

  myapp:
    build: .
    restart: "no"
    depends_on:
      - db
    links:
      - "db:database"
    ports:
      - 8080:8080